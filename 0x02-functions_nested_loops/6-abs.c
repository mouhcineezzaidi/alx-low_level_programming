#include "main.h"
/**
 *_abs - return the absolute value of a given integer
 * @n: the int to check
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	return (n);
}
